#include <stdio.h>

int main() {
    float num1, num2, num3; 
    printf("Digite três números: ");
    scanf("%f %f %f', &num1, &num2, &num3);

    if (num1 >= num2 && num1 >= num3) {
        printf(")
}